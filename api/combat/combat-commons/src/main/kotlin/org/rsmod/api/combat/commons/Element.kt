package org.rsmod.api.combat.commons

/** Element carried by an attack. [NONE] preserves the legacy combat behaviour. */
public enum class Element {
    NONE,
    PHYSICAL,
    FIRE,
    WATER,
    ICE,
    BLOOD,
    SHADOW,
    SMOKE,
}
